# ============================================================
# Planroom Genius - Environment Configuration
# Copy this file to .env and fill in your credentials
#   cp .env.example .env && nano .env
# ============================================================

# --- AI / API Keys ---
# Google Gemini API key (required for PDF analysis & knowledge scoring)
# Get yours at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# --- Planroom Credentials ---

# BuildingConnected (uses browser login to capture JWT)
SITE_LOGIN=your_email@domain.com
SITE_PW=your_password

# Bidplanroom / ConstructConnect SSO
BIDPLANROOM_EMAIL=your_email@domain.com
BIDPLANROOM_PW=your_password

# Loyd Builds Better (Squarespace paywall)
LOYD_LOGIN=your_email@domain.com
LOYD_PW=your_password

# PlanHub (optional override - defaults to SITE_LOGIN/SITE_PW)
# PLANHUB_LOGIN=your_email@domain.com
# PLANHUB_PW=your_password

# --- Browser Settings ---

# Run Chromium headless (no GUI). Set true for Pi / server, false for local dev.
HEADLESS=true

# Chrome profile path (auto-detected, override if needed)
# Linux/Pi: ~/.config/chromium
# Windows:  auto-uses local persistent profile
# CHROME_USER_DATA_DIR=/home/pi/.config/chromium
# CHROME_PROFILE_NAME=Default

# --- Google Drive Integration (optional) ---

# Enable Google Drive file backup
USE_GOOGLE_DRIVE=false
# Place your credentials.json from Google Cloud Console in the backend/ directory

# --- Debugging ---
# PLANHUB_DEBUG=false
# BC_DEBUG=false
# ANONYMIZED_TELEMETRY=false
